apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test-ingress
  namespace: dev
spec:
  rules:
  - host: 35.186.159.138.xip.io
    http:
      paths:
      - backend:
          serviceName: hello-service
          servicePort: 80
